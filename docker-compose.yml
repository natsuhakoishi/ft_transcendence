services:
  app:
    build: ./app/
    container_name: ft_klbq
    ports:
        - "4321:4321"
        - "1234:1234"
    networks:
        - klbq_network
    volumes:
        - ./db_data:/app/database
    restart: always

networks:
    klbq_network:
